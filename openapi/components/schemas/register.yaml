title: Register
type: object
required:
  - name
  - email
  - password
properties:
  status:
    type: string
    enum:
      - Success
  state_code:
    type: integer
    enum:
      - 200
  deprecated:
    type: boolean
    enum:
      - false
  message:
    type: string
  data:
    $ref: .\token.yaml
